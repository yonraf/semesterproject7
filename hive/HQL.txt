CREATE EXTERNAL TABLE default.json1(username string, repos integer, `followers`integer, `following`integer, created_at Date) ROW FORMAT SERDE 'org.openx.data.jsonserde.JsonSerDe' STORED AS TEXTFILE;

LOAD DATA INPATH 'hdfs://namenode:9000/users/userTest.json' OVERWRITE INTO TABLE json1;

SELECT * FROM json1 WHERE repos IN (SELECT Max(repos) from json1)

select username, repos from json1;


SET hive.execution.engine=tez;

SELECT username, repos FROM json1 where (select max(repos) from json1);